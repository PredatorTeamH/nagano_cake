<div class="container">
  <div class="row">
    <h2><strong>配送先登録/一覧</strong></h2>
  </div>
  <div class="row">
      <!--配送先登録フォーム-->
      <%= form_with model:@address_new do |f| %>
      <table class="table">
        <tr>
          <td>郵便番号</td>
          <td><%= f.text_field :zip_code, class: "form-control" %></td>
        </tr>
        <tr>
          <td>住所</td>
          <td><%= f.text_field :address, class: "form-control" %></td>
        </tr>
        <tr>
          <td>宛名</td>
          <td><%= f.text_field :name, class: "form-control" %></td>
        </tr>
        <tr>
          <td><%= f.submit "新規登録", class: "btn btn-sm btn-success" %></td>
        </tr>
      </table>
      <% end %>

      <!--配送先一覧-->
      <table class="table">
        <thead>
          <tr>
            <th>郵便番号</th>
            <th>住所</th>
            <th>宛名</th>
            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
          <% @addresses.each do |address| %>
            <tr>
              <td><%= address.zip_code %></td>
              <td><%= address.address %></td>
              <td><%= address.name %></td>
              <td>
                <%= link_to "編集する", edit_shipping_address_path(address), class: "btn custom-button" %>
                <%= link_to "削除する", shipping_address_path(address), method: :delete, class: "btn delete-button" %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>